# 改法确认稿 — 等您拍板

> 2026-09-05。输入：`evo2/results/EVOTRACE_阶段总结_v7判决与互补组合_20260905.md`
>（当前真实进度）× `恢复方案_回到主线.md`（目标形态）。本文回答两问：
> **改成什么样**（终态）与**打算怎么改**（步骤）。确认后按此执行，不再改口径。

---

## 一、颗粒度对齐（进度总结 → 恢复框架的映射）

| 进度总结 §（9/5 颗粒度） | 恢复框架定位（主线/战役颗粒度） | 说明 |
|---|---|---|
| §2 v7 判决：act/dual/dcat 全超 v4，sep 输 | **战役线**科学结论，留在 campaigns/ | "DL 输是标签噪声、n100 后反转"是重要教训，进复用方案资产表 |
| §3 sep 根因 + A1 有效/A2 排除 | **战役线**诊断记录 | A1 的"显式对比目标"思想将来可作机制提案素材，现不动 |
| §4 v4×DL 互补组合 min(+0.630) | **这是主线等的资产**：就是漏斗 mid 层的"分工互补打分器"原型 | 概念验证已通过，差"封装成接口"——即总结 §6 唯一 ⏳ 项 |
| §6 MD 战役（9/7 收官） | **战役线**，进行中，**迁移不许碰** | 集群作业只认远端路径，本地搬文件不影响；但涉及脚本留副本 |
| §7 下一步 1（MD 判读） | 战役线，9/7 后做 | 与主线无耦合 |
| §7 下一步 2（互补工具函数落地） | **主线第一个合入物**：走机制提案门进 engine | 本确认稿 Phase 2 |
| §7 下一步 3（工具闭环定稿） | 战役线文档，最终报告落在 campaigns/ppri_evo/docs/ | 引用主线的 CombinedScorer 接口即可 |

**一句话对齐**：9/5 总结里的"工具" = PprI 预测/提名工具（战役工具）；恢复方案的"工具" =
数字定向进化模拟器（主线工具）。互补组合是前者刚刚定型的心脏，也是后者漏斗 mid 层
缺了很久的那块——**两边共用同一个引擎零件，这就是最大化利用的落点**。

## 二、改成什么样（终态）

```
A:\claudework\
├── evotrace_restore\            # 宪法层（已建）：方案×3 + reference 原文 + 本确认稿
├── evo2\                        # 主线：模拟器
│   ├── engine\
│   │   ├── ...（现有内核不动）
│   │   └── surrogate_oracle.py  # ★ 新增 CombinedScorer：min(rank(DL_act), rank(v4_sep))
│   │                            #   可插拔双代理接口 + 单元测试 —— 互补组合的正门落点
│   ├── benchmarks\              # 只留 b1–b7 核心 + figures + chpc_util；战役脚本全迁出
│   ├── results\                 # 进度总结与判决报告（主线文档，保留）
│   └── cluster\
├── campaigns\                   # 战役隔离区
│   ├── ppri_evo\                # 根目录 ppri_evo\ 整体迁入 + evo2/benchmarks 里的战役脚本
│   ├── nom2\                    # nom2 相关
│   └── scratch\                 # 根目录散件 + 原 scratch 122 个
└── docs\                        # 技术方案 v1/v2 正本在此（根目录两份加指向说明）
```

改完后您会看到的样子：根目录干净到只剩 5 个条目；`evo2/` 里每一层都能用 v1 验收线
回答"我为什么在这"；PprI 工具的下一轮提名（MD 收官后）调的是 `evo2.engine` 的正式
接口而不是复制脚本；新蛋白战役（如 nom2 之后的）从一句自然语言开始，而不是从抄
envspec.json 开始。

## 三、打算怎么改（四个 Phase，每步可单独叫停）

### Phase 0 — 冻结快照（10 分钟）
- git add 全部当前状态（含未跟踪的 v7/互补脚本），commit + tag `pre-restore-20260905`。
- 目的：任何搬动出错都可一键回滚。

### Phase 1 — R0 迁移（约 1 小时，机械操作）
- 按 `恢复方案_回到主线.md` §4 清单执行 git mv：根目录散件→campaigns/scratch/；
  ppri_evo→campaigns/ppri_evo/；evo2/benchmarks 战役脚本→campaigns/ppri_evo/scripts/。
- **例外（不迁，留主线）**：`train_sep_model_v4.py`、`train_dl_v7_par.py`、
  `ablate_v7_sep.py`、`verify_combine_v4_dl.py`、`postproc_a1_ep24.py` —— 这是互补
  组合的证据链与实现底座，Phase 2 要从中提炼接口；迁完再定去留。
- 结束后 git commit + tag `restore-point-20260905`，README 重写为 v1 定位第一句。
- **MD 战役安全性**：所有集群作业在远端，本地移动零影响；`scan_*`/`fetch_*` 类脚本
  在 campaigns/ 下照常可用。

### Phase 2 — 互补组合走正门（半天，本确认稿的核心增量）
1. 写机制提案 `evotrace_restore/提案_互补双代理打分器.md`（四段式：实战问题→证据
   min 组合 +0.630 与 RD_POS 反例→一般机制"强度与特异性由不同模型类分工评分"→
   落点 surrogate_oracle）。
2. 实现 `evo2/engine/surrogate_oracle.py`：`CombinedScorer`（DL_act 适配器 + v4_sep
   适配器 + min-rank 组合 + 接口约定"任何未来代理实现同一协议即可换装"）+ 单元测试
   （用 combine_result.csv 真实数字做回归测试：RD_POS 被压后、s13_c1 排第 2）。
3. 9/7 MD 收官后，战役提名直接 `from evo2.engine.surrogate_oracle import CombinedScorer`。

### Phase 3 — 战役线续跑（9/7，与主线并行不阻塞）
- MD 判读、工具闭环定稿、最终报告——全部在 campaigns/ppri_evo/ 内完成，
  产出只通过机制提案门回流主线。

### Phase 4 — 主线新工 R1 启动（MD 期间即可开始，无资源冲突）
- EnvSpec 编译器 v0：schema.py（以 PprI envspec.json 为第一校验实例）→ vocabulary.py
  20 条词表 → compiler.py + CLI 确认界面 → v1 用户故事原句验收。
- 出口标准见路线图 R1。

## 四、请您拍板的三个点

1. **互补接口落主线**：CombinedScorer 进 `evo2/engine/`（推荐）还是先留战役脚本？
   ——推荐进主线，理由：它是唯一已过概念验证、且天然属于漏斗 mid 层的资产。
2. **迁移范围**：Phase 1 清单是否照单全收？特别是根目录两个技术方案 md 保留原地
   （加指向说明）还是也移进 docs/？——推荐保留原地。
3. **R1 时机**：EnvSpec 编译器现在就开工（推荐，MD 在集群跑，本地无冲突），还是等
   9/7 MD 收官后再开？

确认后我从 Phase 0 开始执行。
